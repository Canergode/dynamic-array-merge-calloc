# Dynamic Array Merge With Calloc

This project generates two random arrays of different sizes using calloc and merges them into a third dynamically allocated result array.

## What I Learned
- How to merge two dynamic arrays into a third one
- How to use a counter to track position in the result array
- How to allocate the result array with the combined size
- How to free all three dynamically allocated arrays
